spin-fm (2.6.33) unstable; urgency=medium

  * Route Return and keypad Enter through viewport-local and application event
    filters so Qt item-view handling cannot swallow activation.
  * Open an actually selected row before falling back to a stale current index.
  * Normalize Qt5/Qt6 key codes and retain the view-level activation fallback.
  * Use weak view back-references, avoid duplicate teardown collections, and
    retain only bounded error details when opening large selections.

 -- JJ Posti <techtimejourney.net>  Mon, 27 Jul 2026 00:00:00 +0300

spin-fm (2.6.32) unstable; urgency=medium

  * Fix Return and keypad Enter under PyQt6 by adding the missing key aliases
    to the compatibility layer.
  * Harden the file-view activation check against unavailable key constants.

 -- JJ Posti <techtimejourney.net>  Mon, 27 Jul 2026 00:00:00 +0300

spin-fm (2.6.31) unstable; urgency=medium

  * Open the current selected file or folder with Return or keypad Enter.
  * Handle activation directly in the file view for reliable keyboard routing.

 -- JJ Posti <techtimejourney.net>  Mon, 27 Jul 2026 00:00:00 +0300

spin-fm (2.6.30) unstable; urgency=medium

  * Reduce long-session and large-selection memory use by streaming selection
    ranges, recycling the shared model sooner, caching clipboard availability,
    bounding retained operation details, and capping the icon cache at 8 MiB.
  * Batch Trash requests and keep permanent multi-selection deletion in the
    bounded background file-operation worker.
  * Add cross-instance desktop Cut/Copy/Paste, Copy to Folder, Move to Folder,
    Shift+Delete, mixed-delete confirmation, Keep Both conflict handling, and
    a physical Ctrl-key copy-mode status indicator.
  * Temporarily remove runtime test suites, pytest, and autopkgtest packaging;
    keep syntax, shell, source, and package-hygiene checks under make verify.
  * Keep GUI, udev, and MIME bindings as runtime dependencies rather than
    requiring them merely to assemble the architecture-independent package.

 -- JJ Posti <jjposti@techtimejourney.net>  Mon, 27 Jul 2026 12:30:00 +0300

spin-fm (2.6.29) unstable; urgency=medium

  * Keep the location bar on the current directory when a file or folder is
    single-clicked; selection still updates asynchronous size, MIME, and
    modification details.
  * Coalesce deferred file-information callbacks with owned timers and shorten
    idle worker-thread retention so queued callbacks and native thread stacks
    are released sooner.
  * Cap deferred filesystem-model path tracking, count very large selections
    without materializing one model index per item, and release closed-tab
    actions, models, metadata, and window references during teardown.
  * Import `Worker` explicitly in the PyQt progress-rate regression and add
    coverage for terminal progress delivery, stable location text, bounded
    deferred path tracking, and range-based selection counting.

 -- JJ Posti <jjposti@techtimejourney.net>  Mon, 27 Jul 2026 09:15:00 +0300

spin-fm (2.6.26) unstable; urgency=medium

  * Publish the worker payload-disposal marker before the private completion
    signal, so `active_count == 0` cannot be observed while `_disposed` is
    false.
  * Track callback and signal-object teardown with a separate idempotence guard,
    preserving public finished delivery while still disconnecting retained
    closures exactly once.
  * Restore bounded error text and rate-limited progress events, and add a
    regression matching the completed-payload lifecycle failure.

 -- JJ Posti <jjposti@techtimejourney.net>  Mon, 27 Jul 2026 07:40:00 +0300

spin-fm (2.6.25) unstable; urgency=medium

  * Route completed-worker cleanup through a dedicated queued signal to the
    TaskManager thread instead of a zero-delay timer created in a QThreadPool
    thread without an event loop.
  * Dispose workers before removing their final active-task state, so payload
    release, callback disconnection, signal-object deletion, and `_disposed`
    complete before the manager reports itself idle.
  * Keep public finished callbacks ordered before internal cleanup and add
    regression checks that prohibit the timer/partial callback path.

 -- JJ Posti <jjposti@techtimejourney.net>  Mon, 27 Jul 2026 06:09:00 +0300

spin-fm (2.6.24) unstable; urgency=medium

  * Recycle the shared filesystem model after a bounded number of distinct
    navigations and after closed tabs, preserving open paths, ordinary
    selections, and scroll positions while releasing stale directory caches
    and watchers.
  * Reuse icon delegates, detach closed-tab callbacks and widgets, delete
    temporary menus and dialogs, and cap application pixmap and stylesheet
    caches.
  * Dispose completed worker signal objects and callbacks, keep recursive
    folder-scan identity tracking bounded, and avoid strong fallback references
    to closed file views.
  * Delete retired MPRIS adaptors and disconnect and detach Qt Multimedia
    backends before deferred QObject destruction.
  * Add regression coverage for bounded scans, non-retaining view fallback,
    shared-model recycling, worker disposal, adaptor cleanup, and cache limits.

 -- JJ Posti <jjposti@techtimejourney.net>  Mon, 27 Jul 2026 05:30:00 +0300

spin-fm (2.6.23) unstable; urgency=medium

  * Render icon-view file and folder names with a dedicated wrapped-text painter so Qt styles cannot replace any part of a long name with an ellipsis.
  * Keep variable item heights synchronized with the exact text rectangle, including unbroken long names that must wrap anywhere.
  * Make ordinary drag and drop move items while Ctrl+drag copies them between tabs, windows, or separate Spin FM instances.
  * Advertise both copy and move drag actions and add regression coverage for full-name painting and modifier-aware transfers.

 -- JJ Posti <jjposti@techtimejourney.net>  Mon, 27 Jul 2026 12:00:00 +0300

spin-fm (2.6.22) unstable; urgency=medium

  * Restore and require the canonical `.gitattributes` distribution policy.
  * Validate `.gitattributes`, `.gitignore`, and Debian source exclusions before building source archives.
  * Require both Git policy files in every published source ZIP.
  * Add regression coverage for missing or incomplete distribution-policy files.

 -- JJ Posti <jjposti@techtimejourney.net>  Mon, 20 Jul 2026 12:00:00 +0300

spin-fm (2.6.21) unstable; urgency=medium

  * Align file-information tests with the intentional MiB-only status format.
  * Keep selected paths in the location bar rather than duplicating them in status text.
  * Preserve asynchronous metadata inspection while verifying queued click handling.
  * Keep source tests usable when a download tool omits dotfiles.
  * Replace the fragile application-wide mouse filter with direct, one-time `clicked(QModelIndex)` bindings for every existing and future file tab.
  * Add cancellable asynchronous recursive folder-size calculation with item counts, mount-boundary protection, and partial-result reporting.
  * Add a normal right-elided file-information status field that keeps the selected size first, yields to core operation messages, and returns afterward.
  * Isolate `python3-magic` failures so a broken MIME binding can never suppress file size or modification time.
  * Add file, folder, direct-signal, persistent-status, real-click, and new-tab regression coverage.

 -- JJ Posti <techtimejourney.net>  Mon, 20 Jul 2026 12:00:00 +0300

spin-fm (2.6.19) unstable; urgency=medium

  * Make main.py the only application bootstrap and remove stale references to
    the nonexistent extension_main.py from the launcher and Debian package.
  * Attach the independent file-information module through an explicit window
    setup callback instead of replacing MainWindow at runtime.
  * Support python3-magic class, module, structured-result, and legacy cookie
    APIs, including MIME encodings and older bytes-path implementations.
  * Add MIME compatibility and unified-launcher regression tests.

 -- JJ Posti <jjposti@techtimejourney.net>  Sun, 19 Jul 2026 23:59:00 +0300

spin-fm (2.6.18) unstable; urgency=medium

  * Preserve executable permissions across GitHub Actions artifact downloads by
    uploading one tar file without artifact re-zipping and verifying it in a
    separate download job.
  * Add an interpreter-driven permission repair/check tool and make every check,
    test, and Debian build restore the audited executable set before running.
  * Make source ZIP permissions independent of checkout modes, so archives built
    after GitHub web upload or artifact download retain canonical `0755` scripts
    and `0644` data files.
  * Add mode-loss, source-archive, Makefile, and GitHub workflow regression tests
    plus dedicated GitHub Actions documentation.

 -- JJ Posti <jjposti@techtimejourney.net>  Sun, 19 Jul 2026 23:55:00 +0300

spin-fm (2.6.17) unstable; urgency=medium

  * Replace per-tab `clicked(QModelIndex)` connections with one application-level
    Qt event filter.
  * Make all existing and future file tabs work automatically without tab scans,
    identity registries, or per-view callback storage.
  * Ignore drag releases using Qt's configured drag-distance threshold while
    leaving normal selection, double-click, and drag behavior untouched.
  * Add real mouse-event regression tests proving that artificial `clicked`
    emissions are no longer the integration path.

 -- JJ Posti <jjposti@techtimejourney.net>  Sun, 19 Jul 2026 23:30:00 +0300

spin-fm (2.6.16) unstable; urgency=medium

  * Integrate the independent file-information module with the real Qt
    `clicked(QModelIndex)` signal.
  * Bind every existing tab during startup and each newly activated tab at
    runtime without editing `main.py`, `app.py`, `main_window.py`, or `tabs.py`.
  * Prevent duplicate view connections and release per-tab callback references
    when tabs are destroyed or the application shuts down.
  * Add Qt regression tests that emit actual click signals in the first tab and
    a newly created tab and verify the displayed file size.

 -- JJ Posti <jjposti@techtimejourney.net>  Sun, 19 Jul 2026 22:30:00 +0300

spin-fm (2.6.15) unstable; urgency=medium

  * Restore click-selected path, size, modification time, and MIME details
    through an independent bounded background module.
  * Integrate the legacy parent callback at runtime while keeping main.py,
    app.py, main_window.py, and tabs.py unchanged.

 -- JJ Posti <jjposti@techtimejourney.net>  Sun, 19 Jul 2026 21:30:00 +0300

spin-fm (2.6.14) unstable; urgency=medium

  * Prevent the second or mounted-volume Trash chooser row from inheriting
    a white system alternate-row color before it is selected.

 -- JJ Posti <jjposti@techtimejourney.net>  Sat, 18 Jul 2026 12:31:00 +0300

spin-fm (2.6.13) unstable; urgency=medium

  * Remove the redundant view-scoped Ctrl+T action so the single window-level
    New Tab shortcut no longer triggers Qt's ambiguous-shortcut warning.

 -- JJ Posti <jjposti@techtimejourney.net>  Sat, 18 Jul 2026 11:47:00 +0300

spin-fm (2.6.12) unstable; urgency=medium

  * Replace the compact Trash selector with a large, resizable chooser that
    shows clear location names and complete home or mounted-volume paths.
  * Keep F9 device actions visible with a fixed action column, bounded sidebar
    sizing, and a remembered width when the sidebar is hidden and restored.
  * Prefer Adwaita for first-run icons with installed-theme fallbacks and style
    the new production dialogs consistently across every Spin FM theme.
  * Remove retired compatibility wrappers, release worker payloads promptly,
    stream fallback Trash cleanup, normalize archive permissions, and align both
    project changelogs.

 -- JJ Posti <jjposti@techtimejourney.net>  Fri, 17 Jul 2026 22:30:00 +0300

spin-fm (2.6.11) unstable; urgency=medium

  * Discover and open existing home and mounted-device Trash locations,
    supporting both freedesktop USB Trash layouts without creating them.
  * Keep direct Home Trash navigation when no mounted Trash is present.

 -- JJ Posti <jjposti@techtimejourney.net>  Fri, 17 Jul 2026 22:00:00 +0300

spin-fm (2.6.10) unstable; urgency=medium

  * Fix Trash toolbar navigation when Qt has not indexed the hidden
    freedesktop Trash directory hierarchy.

 -- JJ Posti <jjposti@techtimejourney.net>  Fri, 17 Jul 2026 20:10:00 +0300

spin-fm (2.6.9) unstable; urgency=medium

  * Make the Trash toolbar open Trash, use GIO for complete desktop-trash
    emptying, and permanently delete items already inside Trash folders.
  * Make local drag-and-drop a confirmed Cut/Paste-style move with same-name
    overwrite or skip prompts and clean cancellation.
  * Document complete PyQt 5 source dependencies while retaining PyQt 6 for
    Debian packaging and production defaults.

 -- JJ Posti <jjposti@techtimejourney.net>  Fri, 17 Jul 2026 18:25:30 +0300

spin-fm (2.6.8) unstable; urgency=medium

  * Register the embedded player through MPRIS for playerctl, desktop media
    controls, and Wayland_OSD; publish Alt+P and button state immediately.
  * Show complete wrapped file and folder names and add native LWM Dark and
    LWM Graphite application themes.
  * Simplify Debian automation to make check, make tests, make deb, and make
    all; remove Ruff, retired packaging files, and generated legacy artifacts.
  * Keep launch, test, archive, and Debian staging paths bytecode-free while
    treating optional OSD and D-Bus failures as non-fatal.

 -- JJ Posti <jjposti@techtimejourney.net>  Fri, 17 Jul 2026 14:45:00 +0300

spin-fm (2.6.7) unstable; urgency=medium

  * Make Alt+P deterministic when Qt Multimedia state reporting is delayed.
  * Route audio shortcuts before child widgets and emit immediate OSD feedback.
  * Standardize GPL-2.0-or-later metadata and tighten release validation.

 -- JJ Posti <jjposti@techtimejourney.net>  Fri, 17 Jul 2026 01:05:44 +0300

spin-fm (2.6.6) unstable; urgency=medium

  * Fix application-wide Alt+P Wayland_OSD play/pause feedback.
  * Harden cache-free Debian releases, remove legacy artifacts, and focus docs.

 -- JJ Posti <jjposti@techtimejourney.net>  Thu, 16 Jul 2026 23:22:11 +0300

spin-fm (2.6.5) unstable; urgency=medium

  * Make supported workflows cache-free and add deterministic source validation.
  * Reject Python-index packaging metadata and generated distribution artifacts.

 -- JJ Posti <jjposti@techtimejourney.net>  Thu, 16 Jul 2026 04:50:47 +0300

spin-fm (2.6.4) unstable; urgency=medium

  * Remove PyPI packaging and install application-private source directly.
  * Harden optional Wayland_OSD discovery, socket validation, and startup retries.

 -- JJ Posti <jjposti@techtimejourney.net>  Thu, 16 Jul 2026 04:37:00 +0300

spin-fm (2.6.3) unstable; urgency=medium

  * Restore double-click activation and expand the seekable embedded player.
  * Add failure-isolated Wayland_OSD media and volume feedback.

 -- JJ Posti <jjposti@techtimejourney.net>  Thu, 16 Jul 2026 03:52:37 +0300

spin-fm (2.6.2) unstable; urgency=medium

  * Add Ctrl+Up parent navigation and lazy embedded audio playback.
  * Add themed controls, external-player fallback, and Qt5/Qt6 tests.

 -- JJ Posti <jjposti@techtimejourney.net>  Wed, 15 Jul 2026 23:55:00 +0300

spin-fm (2.6.1) unstable; urgency=medium

  * Harden launching, storage caching, task cleanup, and overwrite safety.
  * Add symlink-aware operation checks and broader runtime coverage.

 -- JJ Posti <jjposti@techtimejourney.net>  Wed, 15 Jul 2026 23:30:00 +0300

spin-fm (2.6.0) unstable; urgency=medium

  * Add asynchronous operations, bounded caches, refreshed themes, and packaging.

 -- JJ Posti <jjposti@techtimejourney.net>  Wed, 15 Jul 2026 12:00:00 +0300
